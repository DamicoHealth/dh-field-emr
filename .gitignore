node_modules/
dist/
.DS_Store
*.dmg
*.zip
*.blockmap

# Local data dumps — contain REAL patient PHI. Never commit (repo is public).
exports/

# SwiftUI native build artifacts (73MB); source is kept, build output is not
packages/native/build/

# Marketing site is its own standalone git repo (custom domain) — keep it separate
packages/damicohealth-com

# iCloud/Finder conflict copies (repo lives under a synced Desktop) — never commit/build
* [0-9].*
* [0-9][0-9].*
* copy.*
* copy [0-9].*
